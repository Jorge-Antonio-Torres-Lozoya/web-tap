import { Environment } from './environment.model';

// Production environment
export const environment: Environment = {
  production: true,
  apiBaseUrl: 'https://api-tap-production.up.railway.app/api/v1',
};
